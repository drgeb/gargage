---
- name: install pgdg repository
  become: true
  package:
    name: "{{ postgresql_repo_rpm }}"
    state: present

- name: install packages
  become: true
  package:
    name: "{{ item }}"
    state: present
  with_items:
    - "{{ postgresql_packages }}"
