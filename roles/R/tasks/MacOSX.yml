---
- name: install packages
  homebrew:
    name: "{{ R_packages }}"
    state: present

- name: install rstudio
  become: false
  homebrew_cask:
    name: rstudio
    state: present
  when: R_rstudio_enabled

- name: include asdf tasks
  include: asdf.yml
