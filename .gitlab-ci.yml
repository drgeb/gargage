---
image: ruby:2.5

stages:
  - test

services:
  - name: docker:dind

Kitchen Tests:
  stage: test
  script:
    - gem install bundler
    - bundle install
    - kitchen test ubuntu18
  tags:
    - docker
